[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA: React client-side routes (e.g. password reset ?resetToken=...)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
