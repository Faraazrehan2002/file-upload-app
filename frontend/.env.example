# Local dev: leave unset — Vite proxies /api to http://localhost:5001
# Netlify: set in Site settings → Environment variables (build time)
# Example: https://your-backend.onrender.com/api
VITE_API_URL=
